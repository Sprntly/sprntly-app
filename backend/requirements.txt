fastapi==0.115.0
uvicorn[standard]==0.32.0
# Upper bound added 2026-08-25. The floor was the only bare `>=` in this file,
# and `deploy-backend.yml` runs `pip install -r requirements.txt` on EVERY
# deploy — so when 1.0.0 shipped, a deploy walked across a major on its own.
# 1.0.0 removed `temperature` from `messages.create()`, every planner call
# raised TypeError, and `plan_for_answer` caught it and logged "answering
# unplanned": the product kept answering, without a router, out of the
# knowledge graph.
#
# The code is now major-agnostic — `llm._anthropic_kwargs` sends `temperature`
# through `extra_body`, which both majors accept — so this ceiling is hygiene,
# not a workaround: it stops the NEXT major arriving the same silent way. Raise
# it deliberately, with `tests/test_anthropic_sdk_compat.py` green against the
# new version.
anthropic>=0.55.0,<2.0.0
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.6.0
PyJWT==2.9.0
python-multipart==0.0.12
python-docx==1.1.2
python-pptx==1.0.2
openpyxl==3.1.5
pypdf==5.1.0
cryptography==44.0.0
google-auth==2.36.0
google-auth-oauthlib==1.2.1
google-api-python-client==2.154.0
# 2.16.0: accepts the new sb_secret_/sb_publishable_ API key formats.
# 2.10.0 raised SupabaseException("Invalid API key") on the new keys, blocking
# rotation off the (compromised) legacy JWT keys. 2.16.0 is the HIGHEST supabase
# that still resolves against our pinned pydantic==2.9.2 (2.17+ require a newer
# pydantic). Validated on Py3.11 against the real project: full requirements
# install (no pydantic conflict), create_client accepts an sb_secret_ key, a live
# select succeeds, and auth.admin + storage.from_ + rpc are all present.
# NOTE: CI mocks Supabase (FakeSupabaseClient), so the runtime lib bump was
# validated out-of-band, not by pytest. See the 2026-06-18 secret-rotation incident.
supabase==2.16.0
argon2-cffi==23.1.0
playwright==1.48.0
apscheduler>=3.10,<4
# IANA timezone database for zoneinfo (app/brief_schedule.py resolves each
# company's Monday-09:00 send time in its configured tz). macOS/most distros
# ship the system tzdb, but slim Linux containers don't — pin it so the weekly
# brief's tz/DST logic is correct everywhere.
tzdata>=2024.1
networkx>=3.0
beautifulsoup4>=4.12
# Markdown → HTML for REPORT bodies. Every report pipeline answers in markdown
# (the pinned HTML templates went with #1024), and a report is now a rich
# document you edit in the panel like a PRD — so what is stored and served has
# to be HTML. There was no converter anywhere in this repo, front or back.
# Pure Python, no transitive dependencies, and it ships the two extensions
# these documents actually need (`tables`, `fenced_code`). Its output is
# SANITISED before it is stored or served — see app/report_markdown.py.
markdown>=3.5
httpx>=0.27
# Error tracking. The [fastapi] extra pulls the Starlette/FastAPI integrations
# which auto-capture unhandled request-handler exceptions. Init is gated on
# SENTRY_DSN (see app/sentry.py) — a no-op when unset.
sentry-sdk[fastapi]>=2.20,<3
# DS engine (app/ds/engine — the deterministic analysis battery behind the
# chat "analyze my data" command): dataframe ops, stats gates (Mann-Whitney,
# chi-square, BH-FDR), and the logistic-regression shadow-removal dedup.
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
# Chart recompression for the DS Claude analysis report (app/ds/claude_analysis
# _shrink_png): an oversized matplotlib PNG is downscaled and re-encoded so it
# fits the embed budget instead of being dropped from the report. Pillow was
# already present transitively, which meant the path silently worked in dev and
# would have been INERT on a clean install — hence the explicit declaration.
# A floor rather than an exact pin: we use only Image.open/convert/resize/save,
# an API stable across majors, so pinning would buy churn instead of safety.
# The floor is a security one — this code decodes image bytes produced inside the
# model's sandbox, so it must not run on a Pillow with known decoder CVEs;
# >=10.3 clears CVE-2024-28219 (_imagingcms buffer overflow, fixed in 10.3.0).
Pillow>=10.3
