# Sign-In Flow

## Overview
Add an email + password sign-in screen.

## Acceptance Criteria
- Email field validates format
- Password field min 8 chars
- Submit shows a loading state

:::design
platform_hint: both
notes: keep the layout centred; use the default Tailwind palette
:::
