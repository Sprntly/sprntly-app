"""Synthesis Agent package.

Hosts the KG write-event hooks (kg_hooks) that the PRD generator and
Synthesis Agent call when state changes. The hooks are stubs today and
will be wired to FalkorDB + Graphiti in a follow-up PR (KG infra).
"""
