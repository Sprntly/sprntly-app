-r requirements.txt
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-xdist==3.8.0
httpx==0.27.2
# Test-only. Pins the CIR / public-feedback REPORT_DATA fixtures to their
# forced-tool schemas so a test cannot pass against a payload the model could
# not have produced — which is how the empty-structured-half bug survived its
# own suite. Present locally only as an altair transitive, so it must be
# declared here or CI fails with ModuleNotFoundError.
jsonschema==4.26.0
