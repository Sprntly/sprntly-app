node_modules
.next
# Verification builds get their own dir so they cannot clobber a running
# dev server's .next — see NEXT_DIST_DIR in next.config.ts.
.next-verify
out
.DS_Store
*.tsbuildinfo
next-env.d.ts
.env*.local
